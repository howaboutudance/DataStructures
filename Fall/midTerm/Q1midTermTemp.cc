/*
 * Michelle Bergin
 * DataStructures
 * Q 1 revised
 */

#include <iostream>
#include "List.h"
void reverse(List<int> &l){
	int count = l.size();
	int begin = 0;
	for(List<int>::iterator it = l.begin(); it != l.end((); it++){
		int temp = *it;
		if (begin = 
	}
}
